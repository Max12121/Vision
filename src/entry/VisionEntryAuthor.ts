// Represents an entry author.
export type VisionEntryAuthor = {
    // Used as author name.
    name?: string;

    // Used as author email.
    email?: string;

    // Used as author URI.
    uri?: string;
};