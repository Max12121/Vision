export type VisionEntryAuthor = {
    // Represents the author name.
    readonly name: string;

    // Represents the author email.
    readonly email: string;

    // Represents a URI related to the author.
    readonly uri: string;
};